# busstop-app

A bus stop application.

## Status

Early scaffold — no application code yet.

## Getting started

_TODO: document install and run steps once the stack is chosen._

## Development

CI runs on every push and pull request to `main` via GitHub Actions
(see `.github/workflows/ci.yml`). The build steps are placeholders and
should be filled in when the project gains real code.

